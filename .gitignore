# Dependencies
node_modules/

# Secrets and local overrides (commit `.env.example` templates only)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Build / test artifacts (align with turbo.json: dist/**, build/**, .svelte-kit/**, coverage/**, generated/**)
dist/
build/
coverage/
.svelte-kit/

# Turborepo (cache + per-package logs)
.turbo/
turbo-*.log

# Playwright (default output dirs when reports/traces are enabled)
playwright-report/
test-results/

# Prisma: generated clients and local SQLite working dirs
packages/prisma/generated/
packages/prisma/.local/
*.db
*.db-journal

# Rust
.cargo-local/

# Tauri
apps/desktop/src-tauri/gen/schemas/

# Environment variables
!.env.schema
.env.local
.env.*.local
env.d.ts