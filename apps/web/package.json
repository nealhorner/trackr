{
  "name": "@trackr/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo \"(scaffold) web dev not configured yet\"",
    "build": "echo \"(scaffold) web build not configured yet\"",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"(scaffold) lint not configured yet\"",
    "e2e": "echo \"(scaffold) no web e2e yet\""
  },
  "dependencies": {},
  "devDependencies": {}
}
